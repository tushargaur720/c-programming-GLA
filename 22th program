// 24. ALTERNATE MATHOD BY FOR loop
#include<stdio.h>
int main(){
    int sum=0,i=1,n;
    printf("enter the value of number:");
    scanf("%d",&n);
    for(i=1;i<=n;i++){
        sum=sum+i;
    }
    printf("sum=%d",sum);
    return 0;
}
